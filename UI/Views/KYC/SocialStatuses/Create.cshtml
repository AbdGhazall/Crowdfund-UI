@using UI.Models.KYC
@model SocialStatus

<h2 class="mb-5 mt-5">Create Social Status</h2>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group mb-4">
        @Html.LabelFor(m => m.Name, "Name")
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Name, null, new { @class = "text-danger" })
    </div>
    <div class="form-group mb-4">
        @Html.LabelFor(m => m.ArabicName, "Arabic Name")
        @Html.TextBoxFor(m => m.ArabicName, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.ArabicName, null, new { @class = "text-danger" })
    </div>
    <button type="submit" class="btn btn-success">Create</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
}

